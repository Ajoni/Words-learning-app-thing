@model Words_learning_app_thing.ViewModels.WordsViewModel

@{
    ViewBag.Title = "Words";
    var words = Model.Words;
}

<h2>Słowa</h2>

<p>
    @Html.ActionLink("Dodaj", "CreateWord", new { languageId = Model.LanguageId })
    | @Html.ActionLink("Wróć", "Languages")
</p>
<table class="table">
    <tr>
        <th>
            Zawartość
        </th>
        <th>
            Język
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Words) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Zawartosc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Jezyk.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edycja", "EditWord", new { id = item.Id }) |
            @Html.ActionLink("Detale", "DetailsWord", new { id = item.Id }) |
            @Html.ActionLink("Usuń", "DeleteWord", new { id = item.Id })
        </td>
    </tr>
}

</table>
